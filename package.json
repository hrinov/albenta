{
  "name": "albena",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start-server": "cd server && npm run start",
    "build-server": "cd server && npm install && npm run build",
    "build-website": "cd website && npm install && npm run build",
    "dev": "concurrently \"cd ./server && npm run dev\" \"cd ./website && npm run dev\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "3.1.0"
  },
  "dependencies": {
    "@ant-design/icons": "^5.3.1",
    "chart.js": "^4.4.1"
  }
}
